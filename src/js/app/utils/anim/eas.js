/*global define $ requestAnimationFrame*/

define(function (require) {
	
	var CellView;

    CellView = Backbone.View.extend({

        initialize: function () {
		
        }

    });

	return CellView;
});
